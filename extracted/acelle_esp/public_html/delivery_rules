#1 standard multipart::-rp "$FROM" -rt "$FROM" -rrr "$FROM" -domain "$DOMAIN" -sub "$SUBJECT" -content-type "multipart/alternative" -enc-type "quoted-printable" -mime-type "text/plain" -disposition inline -attach $text_file -enc-type "8bit" -mime-type "text/html" -disposition inline -attach $html_file -name "$NAME"
#2 standard multipart without "request read receipts" (-rrr)::-rp "$FROM" -rt "$FROM" -domain "$DOMAIN" -sub "$SUBJECT" -content-type "multipart/alternative" -enc-type "quoted-printable" -mime-type "text/plain" -disposition inline -attach $text_file -enc-type "8bit" -mime-type "text/html" -disposition inline -attach $html_file -name "$NAME"
#3 standard multipart without return path (-rp)::-rt "$FROM" -rrr "$FROM" -domain "$DOMAIN" -sub "$SUBJECT" -content-type "multipart/alternative" -enc-type "quoted-printable" -mime-type "text/plain" -disposition inline -attach $text_file -enc-type "8bit" -mime-type "text/html" -disposition inline -attach $html_file -name "$NAME"
#4 standard multipart without Reply-To header (-rt)::-rp "$FROM" -rrr "$FROM" -domain "$DOMAIN" -sub "$SUBJECT" -content-type "multipart/alternative" -enc-type "quoted-printable" -mime-type "text/plain" -disposition inline -attach $text_file -enc-type "8bit" -mime-type "text/html" -disposition inline -attach $html_file -name "$NAME"
#5 standard multipart without (-rt) and (-rp)::-rrr "$FROM" -domain "$DOMAIN" -sub "$SUBJECT" -content-type "multipart/alternative" -enc-type "quoted-printable" -mime-type "text/plain" -disposition inline -attach $text_file -enc-type "8bit" -mime-type "text/html" -disposition inline -attach $html_file -name "$NAME"
